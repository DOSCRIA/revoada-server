generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  id          String @unique @default(uuid())
  name        String
  lastName    String
  email       String @unique
  password    String @unique
  phoneNumber String
}
